[pytest]
markers =
    sherlock: calls real Claude API (~$0.10/run). Run with: pytest -m sherlock
    destructive: requires manual stack teardown first. Run with: pytest --run-destructive
    userguide: validates USER_GUIDE.md flows end-to-end. Run with: pytest -m userguide
