apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timescaledb-data
  namespace: helixobs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: directpv-min-io
  resources:
    requests:
      storage: 100Gi
