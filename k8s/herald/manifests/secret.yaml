apiVersion: v1
kind: Secret
metadata:
  name: herald-secrets
  namespace: helixobs
type: Opaque
stringData:
  # Leave JWT_SECRET empty to disable auth (dev/MVP mode).
  # Set to a comma-separated list of signing secrets to enable JWT enforcement.
  JWT_SECRET: ""
  CHIMEFRB_SLACK_WEBHOOK: ""
  CHIMEFRB_GITHUB_TOKEN: ""
